export const MAX_TASK_CARDS = 20;
export function createTaskCard(cardId) { return { cardId, title: "", description: "", requirements: [""], dueDate: "", dueTime: "17:00", collaboratorIds: [], watcherIds: [], priority: "normal", recurrenceFrequency: null, recurrenceEndsOn: null, attachment: null }; }
export function addTaskCard(cards, cardId) { return cards.length >= MAX_TASK_CARDS ? cards : [...cards, createTaskCard(cardId)]; }
export function removeTaskCard(cards, index) { return cards.length <= 1 ? cards : cards.filter((_, itemIndex) => itemIndex !== index); }
export function validateTaskCards(cards) { for (const [index, card] of cards.entries()) { if (!String(card.title ?? "").trim()) return [{ taskIndex: index, field: "title", message: `Việc ${index + 1} chưa có Tên công việc` }]; if (!String(card.description ?? "").trim()) return [{ taskIndex: index, field: "description", message: `Việc ${index + 1} chưa có Mô tả` }]; const requirements = Array.isArray(card.requirements) ? card.requirements.filter((value) => String(value ?? "").trim()) : []; if (!requirements.length) return [{ taskIndex: index, field: "requirements", message: `Việc ${index + 1} chưa có Yêu cầu` }]; if (!String(card.dueDate ?? "").trim()) return [{ taskIndex: index, field: "dueDate", message: `Việc ${index + 1} chưa có Hạn hoàn thành` }]; if (!String(card.dueTime ?? "").trim()) return [{ taskIndex: index, field: "dueTime", message: `Việc ${index + 1} chưa có Giờ hoàn thành` }]; } return []; }
export function buildBatchPayload({ batchId, departmentId, assigneeId, cards }) { return { mode: "batch", batchId, departmentId, assigneeId, tasks: cards.map((card) => ({ title: String(card.title ?? "").trim(), description: String(card.description ?? "").trim(), requirements: (card.requirements ?? []).map((value) => String(value ?? "").trim()).filter(Boolean), evaluationCriteria: null, dueDate: card.dueDate, dueTime: card.dueTime, priority: card.priority, collaboratorIds: [...(card.collaboratorIds ?? [])], watcherIds: [...(card.watcherIds ?? [])], recurrenceFrequency: card.recurrenceFrequency, recurrenceEndsOn: card.recurrenceEndsOn })) }; }
