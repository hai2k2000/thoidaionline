"use client";
import { useEffect, useState } from "react";
type Task={id:string;title:string;status:string;dueDate:string|null;score:number|null}; type Row={id:string;name:string;department:string;total:number;done:number;overdue:number;score:number|null;tasks:Task[]};
export default function PublicEvaluationSummary(){const[rows,setRows]=useState<Row[]>([]);const[open,setOpen]=useState<string|null>(null);useEffect(()=>{fetch("/api/public-evaluation-summary").then(r=>r.json()).then(d=>setRows(d.rows??[]));},[]);return <main className="min-h-screen bg-slate-50 p-4"><div className="mx-auto max-w-6xl rounded-2xl border bg-white p-5 shadow-sm"><h1 className="text-2xl font-bold">TỔNG HỢP ĐÁNH GIÁ NHÂN VIÊN</h1><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[800px] border-collapse text-sm"><thead><tr className="border-b bg-slate-100 text-left"><th className="p-2">STT</th><th className="p-2">Tên nhân viên</th><th className="p-2">Tổng số CV</th><th className="p-2">Hoàn thành</th><th className="p-2">Quá hạn</th><th className="p-2">Tổng số điểm</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.id} onClick={()=>setOpen(open===r.id?null:r.id)} className="cursor-pointer border-b hover:bg-orange-50"><td className="p-2">{i+1}</td><td className="p-2 font-semibold">{r.name}<span className="block text-xs font-normal text-slate-500">{r.department}</span>{open===r.id&&r.tasks.map(t=><div key={t.id} className="mt-2 rounded border bg-white p-2 text-xs font-normal">{t.title} · {t.status} · {t.dueDate??"—"} · Điểm: {t.score??"—"}</div>)}</td><td className="p-2 text-center">{r.total}</td><td className="p-2 text-center">{r.done}</td><td className="p-2 text-center">{r.overdue}</td><td className="p-2 text-center font-semibold">{r.score??"—"}</td></tr>)}</tbody></table></div></div></main>}
