"use client";
import { useState } from "react";
export default function WorkScheduleAdminShell({ people, userLabel }: { people: { id: string; full_name: string }[]; userLabel: string }) {
 const [message,setMessage]=useState(""); const [busy,setBusy]=useState(false);
 const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const res=await fetch("/api/work-schedule",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({workDate:f.get("workDate"),startTime:f.get("startTime")||null,endTime:f.get("endTime")||null,title:f.get("title"),location:f.get("location"),notes:f.get("notes"),participantIds:f.getAll("participantIds")})});setMessage(res.ok?"Đã lưu lịch công tác.":"Không thể lưu lịch công tác.");setBusy(false);if(res.ok)e.currentTarget.reset();};
 return <main className="min-h-screen bg-slate-50 p-4 text-slate-900"><div className="mx-auto max-w-3xl"><h1 className="text-2xl font-bold">QUẢN TRỊ LỊCH CÔNG TÁC</h1><p className="mt-1 text-sm text-slate-600">Tài khoản: {userLabel}</p><form onSubmit={save} className="mt-4 grid gap-3 rounded-xl border bg-white p-4 shadow-sm"><label className="grid gap-1 text-sm font-semibold">Nội dung công tác<input name="title" required maxLength={500} className="rounded border p-2 font-normal" /></label><div className="grid gap-3 sm:grid-cols-3"><label className="grid gap-1 text-sm font-semibold">Ngày<input name="workDate" type="date" required className="rounded border p-2 font-normal" /></label><label className="grid gap-1 text-sm font-semibold">Bắt đầu<input name="startTime" type="time" className="rounded border p-2 font-normal" /></label><label className="grid gap-1 text-sm font-semibold">Kết thúc<input name="endTime" type="time" className="rounded border p-2 font-normal" /></label></div><label className="grid gap-1 text-sm font-semibold">Địa điểm<input name="location" maxLength={500} className="rounded border p-2 font-normal" /></label><label className="grid gap-1 text-sm font-semibold">Người tham gia<select name="participantIds" multiple className="min-h-32 rounded border p-2 font-normal">{people.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="grid gap-1 text-sm font-semibold">Ghi chú<textarea name="notes" maxLength={2000} className="rounded border p-2 font-normal" /></label><button disabled={busy} className="rounded bg-orange-600 px-4 py-2 font-semibold text-white">{busy?"Đang lưu…":"Lưu lịch công tác"}</button>{message?<p role="status" className="text-sm">{message}</p>:null}</form></div></main>;
}
