import {redirect} from "next/navigation";import {getSessionUser} from "@/lib/serverSession";import {onlineWorkRepository} from "@/lib/onlineWorkRepository";import OnlineWorkAdminShell from "@/components/OnlineWorkAdminShell";
export default async function Page(){const user=await getSessionUser();if(!user)redirect("/login");if(user.role_code!=="admin")redirect("/online-work");const result=await onlineWorkRepository.reporters();if(!result.ok)throw new Error("Không tải được danh sách phóng viên.");return <OnlineWorkAdminShell userLabel={user.full_name} people={result.people as never}/>;}
