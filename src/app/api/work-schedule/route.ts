import { apiError, apiJson, readJsonObject, requireMutationActor, requireReadActor, rpcFailure } from "@/lib/serverApi";
import { workScheduleRepository } from "@/lib/workScheduleRepository";
const date = /^\d{4}-\d{2}-\d{2}$/; const time = /^(?:[01]\d|2[0-3]):[0-5]\d$/;
export async function GET(request: Request) { const guard = await requireReadActor(); if (!guard.ok) return guard.response; const params = new URL(request.url).searchParams; const from = params.get("from") ?? "2000-01-01"; const to = params.get("to") ?? "2100-12-31"; const participantId = params.get("scope") === "self" ? guard.actor.id : undefined; const result = await workScheduleRepository.list(from, to, participantId); return result.ok ? apiJson({ rows: result.rows }) : rpcFailure(result.error); }
export async function POST(request: Request) { const guard = await requireMutationActor(); if (!guard.ok) return guard.response; const body = (await readJsonObject(request)) ?? {}; const title = typeof body.title === "string" ? body.title.trim() : ""; const workDate = typeof body.workDate === "string" ? body.workDate : ""; const endDate = typeof body.endDate === "string" ? body.endDate : workDate; const planType = body.planType === "business" || body.planType === "event" ? body.planType : "work"; const requestedParticipants = Array.isArray(body.participantIds) && body.participantIds.every((id) => typeof id === "string") ? body.participantIds as string[] : []; const participantIds = guard.actor.role_code === "admin" ? requestedParticipants : [guard.actor.id]; if (!title || title.length > 500 || !date.test(workDate) || !date.test(endDate) || endDate < workDate || participantIds.length > 50) return apiError("invalid_request", 400); const result = await workScheduleRepository.save(guard.actor.id, { id: typeof body.id === "string" ? body.id : undefined, workDate, endDate, planType, startTime: typeof body.startTime === "string" && time.test(body.startTime) ? body.startTime : null, endTime: typeof body.endTime === "string" && time.test(body.endTime) ? body.endTime : null, title, location: typeof body.location === "string" ? body.location.trim().slice(0, 500) || null : null, notes: typeof body.notes === "string" ? body.notes.trim().slice(0, 2000) || null : null, participantIds }, guard.actor.role_code === "admin"); return result.ok ? apiJson({ row: result.row }) : rpcFailure(result.error); }
export async function DELETE(request: Request) { const guard = await requireMutationActor(); if (!guard.ok) return guard.response; const id = new URL(request.url).searchParams.get("id"); if (!id) return apiError("invalid_request", 400); const result = await workScheduleRepository.remove(guard.actor.id, id, guard.actor.role_code === "admin"); return result.ok ? apiJson({ ok: true }) : rpcFailure(result.error); }
