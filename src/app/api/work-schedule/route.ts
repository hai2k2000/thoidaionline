import { apiError, apiJson, readJsonObject, requireMutationActor, requireReadActor, rpcFailure } from "@/lib/serverApi";
import { workScheduleRepository } from "@/lib/workScheduleRepository";
const date = /^\d{4}-\d{2}-\d{2}$/; const time = /^(?:[01]\d|2[0-3]):[0-5]\d$/;
const canViewAllSchedules = (actor: { role_code: string; is_department_manager?: boolean }) => actor.is_department_manager === true || ["admin", "tong_bien_tap", "pho_tong_bien_tap"].includes(actor.role_code);
export async function GET(request: Request) { const guard = await requireReadActor(); if (!guard.ok) return guard.response; const params = new URL(request.url).searchParams; const from = params.get("from") ?? "2000-01-01"; const to = params.get("to") ?? "2100-12-31"; const selfOnly = params.get("scope") === "self" || !canViewAllSchedules(guard.actor); const result = await workScheduleRepository.list(from, to, selfOnly ? guard.actor.id : undefined); return result.ok ? apiJson({ rows: result.rows }) : rpcFailure(result.error); }
export async function POST(request: Request) { const guard = await requireMutationActor(); if (!guard.ok) return guard.response; if (guard.actor.role_code !== "admin") return apiError("forbidden", 403); const body = (await readJsonObject(request)) ?? {}; const title = typeof body.title === "string" ? body.title.trim() : ""; const workDate = typeof body.workDate === "string" ? body.workDate : ""; const participantIds = Array.isArray(body.participantIds) && body.participantIds.every((id) => typeof id === "string") ? body.participantIds as string[] : []; if (!title || title.length > 500 || !date.test(workDate) || participantIds.length > 50) return apiError("invalid_request", 400); const result = await workScheduleRepository.save(guard.actor.id, { id: typeof body.id === "string" ? body.id : undefined, workDate, startTime: typeof body.startTime === "string" && time.test(body.startTime) ? body.startTime : null, endTime: typeof body.endTime === "string" && time.test(body.endTime) ? body.endTime : null, title, location: typeof body.location === "string" ? body.location.trim().slice(0, 500) || null : null, notes: typeof body.notes === "string" ? body.notes.trim().slice(0, 2000) || null : null, participantIds }); return result.ok ? apiJson({ row: result.row }) : rpcFailure(result.error); }
export async function DELETE(request: Request) { const guard = await requireMutationActor(); if (!guard.ok) return guard.response; if (guard.actor.role_code !== "admin") return apiError("forbidden", 403); const id = new URL(request.url).searchParams.get("id"); if (!id) return apiError("invalid_request", 400); const result = await workScheduleRepository.remove(id); return result.ok ? apiJson({ ok: true }) : rpcFailure(result.error); }
