import { apiError, apiJson, readJsonObject, requireMutationActor, rpcFailure } from "@/lib/serverApi";
import { dutyTaskRepository } from "@/lib/dutyTaskRepository";
export async function POST(request: Request) { const guard = await requireMutationActor(); if (!guard.ok) return guard.response; if (guard.actor.role_code !== "admin") return apiError("forbidden", 403); const body = await readJsonObject(request); const rows = Array.isArray(body?.rows) ? body.rows : []; if (!rows.length || rows.length > 100) return apiError("invalid_request", 400); const created: string[] = []; for (const row of rows) { if (!row || typeof row !== "object") return apiError("invalid_request", 400); const input = row as Record<string, unknown>; const values = ["title","description","departmentId","assigneeId","reviewerId","dueDate","dueTime","dutyMonth"]; if (values.some((key) => typeof input[key] !== "string" || !(input[key] as string).trim())) return apiError("invalid_request", 400); const result = await dutyTaskRepository.create(guard.actor.id, input as never); if (!result.ok) return rpcFailure(result.error); created.push(result.id); } return apiJson({ created }, 201); }
