# Include inside the thoidai.online server block's /supa/ location.
# PostgREST echoes long query metadata in response headers for scoped task
# lists; keep the proxy from converting valid responses into 502s.
proxy_buffer_size 16k;
proxy_buffers 8 16k;
proxy_busy_buffers_size 32k;
